# Phase 4 - Interfaces (CLI / n8n / REST)

## Réalisé
- CLI Typer expose `version`, `search`, `read` + options retry/backoff.
- Points d'entrée Poetry : `mcp-odoo`.

## À compléter
- Commandes `create`, `write`, `unlink`, `call`.
- Nodes n8n et API REST (non implémentés, placeholders à prévoir dans `interfaces/`).
